# DEV_LOG — 개발자 노트 (Claude 전용)

유저는 이 파일을 안 봄. **미래의 나(컨텍스트 압축 이후의 Claude)를 위한 기록**이다. 코드만 보고 재구성하기 번거롭거나, 한 번 삽질했던 함정 위주로 적는다. `update_log` 명령 받으면 이 파일과 `USER_PROFILE.md`를 최신 상태로 갱신할 것.

유저 도메인 지식(돈을 어떻게 굴리는지)은 여기 말고 [USER_PROFILE.md](USER_PROFILE.md)에 있음. 이 파일은 순수 엔지니어링 노트.

---

## 스택 / 배포
- DB+인증: **Supabase** (프로젝트 `hvttjltoxweuoqvmevsx`). `index.html`에 anon key 하드코딩돼 있음 — 공개돼도 되는 키(RLS로 보호), 문제 아님.
- 호스팅: **GitHub Pages**, 저장소 `github.com/ssd2am/budget-app` (public — Pages 무료 제약), URL `https://ssd2am.github.io/budget-app/`. main 브랜치에 직접 커밋하는 게 이 프로젝트 관행(따로 브랜치 안 팜).
- **Google Apps Script(`Code.gs`)는 완전히 폐기됨.** 파일이 폴더에 레거시로 남아있지만 절대 참고하지 말 것 — 지금 앱은 순수 정적 `index.html` + `style.css` + Supabase.
- `schema.sql` = 최초 마이그레이션(과거 시드 데이터 포함)만 담고 있고, **그 이후 스키마 변경은 반영 안 됨.** "현재 스키마"로 오인하지 말 것. 이 파일 하단 "스키마 변경 이력"에 이후 변경사항 기록.
- **나는 Supabase에 DDL(스키마 변경)을 직접 못 함** — anon key는 RLS로 인증된 유저의 CRUD만 가능. 스키마 바꿔야 하면 SQL을 만들어서 유저한테 Supabase SQL Editor에서 직접 실행해달라고 요청할 것.
- CSS는 `style.css`로 완전 분리. `index.html`에 인라인 스타일 없음 (지키면서 작업할 것).

## 아키텍처 핵심 패턴
- **`guard(fn)`을 반드시 이해하고 시작할 것.** 모든 서버 액션이 이걸 통해서만 실행됨 — 연타 방지 + "처리 중" 토스트를 한 곳에서 보장. **절대 중첩 호출하면 안 됨**: `loadMonth`/`loadAssets`/`loadPlan` 같은 "다시 불러오기" 함수들은 스스로 `guard()`로 감싸지 않는다. 저장 동작이 이미 `guard()` 안에서 실행 중일 때 그 안에서 새로고침 함수를 또 부르면 `busy=true`라 조용히 무시됨 — "저장은 됐는데 화면 안 바뀜" 버그의 원인이었음(2026-08-11, 실제로 유저가 중복 기록 만듦). 새로고침 함수는 최상위 진입점(초기 로딩·월 이동·탭 전환)에서만 `guard()`로 감싼다.
- **`voucherCache` 레이스 컨디션(2026-08-28 수정)**: 결제수단 칩(`renderPayMethods`)이 전역 캐시를 읽는데, 초기 `loadVouchers()`가 비동기라 로딩 전에 시트를 열면 빈 목록처럼 보였음. `openSheet()`가 열릴 때마다 `loadVouchers().then(renderPayMethods)`로 조용히 재조회하게 고쳐서 해결. **비슷한 캐시-먼저-읽고-나중에-채워지는 패턴 새로 만들 때 이 버그 재발 안 하게 조심.**
- **자산 편집(`editAssetField`, 2026-08-28 리팩터)**: 필드별 개별 프롬프트(용도/상세/금액/분류/비고 각각 탭한 것만 물어봄)로 분리됨. **`editVoucher`(머니)/`editFx`(고정지출)/`editAnnual`(연간고정지출)는 아직 옛날 방식(필드 전부 순차 프롬프트)으로 남아있음 — 다음에 손볼 때 같은 필드별 패턴으로 리팩터할 것.** (2026-08-28 유저가 이 문제 다시 지적함, 처리 예정)
- **스냅샷(`appendSnapshot`)은 하루 최대 1개로 upsert**(같은 날짜 있으면 update, 없으면 insert) — 2026-08-28 이전엔 편집할 때마다 새 row를 insert해서 하루에 여러 개 쌓이는 문제가 있었음. 지금은 `snap_date`로 조회 후 있으면 update.

## 알려진 설계 함정 / 다시 제안하지 말 것
과거에 명시적으로 반려된 기능들(원본 시트 분석 단계, `가계부앱-구현-핸드오프.md` 3절 참고) — 재발 방지용으로 여기 다시 적음:
- 지출 카테고리(식비/의료 등)/지출처/계좌 태그 — 개별 지출에 정체성 부여 안 함. 판단 단위는 항상 "하루 합계".
- 순자산 추이 선그래프, "최근 기록" 리스트, "무지출 N일" 카운터 — 전부 반려됨.
- 자산 도넛은 표 안의 대분류(`group_name`: 현금성자산/투자/장기)가 아니라 **`category`(집계분류) 5종**(현금/주식/적금/퇴직연금/주택청약) 기준으로 그려야 함 — 축을 헷갈리면 안 됨.
- 메모/비고는 "인디케이터 레벨"(점 표시, 탭해서 확인) — 항목명급 위계로 올리면 안 된다는 게 원 설계 원칙. **단, 2026-08-28에 PC(≥768px) 한정으로 지출 메모는 항상 텍스트로 보이게 예외를 만듦**(공간 여유 때문에 유저가 명시적으로 요청) — 모바일은 여전히 점+탭.
- 자산표는 "행 구성이 고정 아님"이 설계 의도 — `category` 5종 검증 외에는 항목을 자유롭게 추가/삭제/이름변경 가능해야 함. 고정 리스트로 박지 말 것.

## 자주 틀리는 도메인 이해 (구현 전에 꼭 확인할 것)
- **"계획" 탭의 Spending ≠ 고정지출.** Spending = 캘린더에 찍히는 순수 재량 지출("달달"). 고정비는 완전 별개 예산, `fixed_cost` 테이블에서만 관리, 캘린더엔 절대 안 찍힘. → 자세한 도메인 구조는 [USER_PROFILE.md](USER_PROFILE.md) 참고. **애매하면 코드 구조만 보고 넘겨짚지 말고 먼저 물어볼 것** — 2026-08-28에 이걸 잘못 짚었다가 유저가 크게 답답해함.
- "월예산"이라는 단어는 이 앱에서 "생활비(용돈) 한도"를 뜻함. 고정비 포함 총예산이 아님.
- 이 프로젝트는 원래 Google Apps Script + Google Sheets로 시작(`가계부앱-구현-핸드오프.md` 참고, 지금은 레거시)했다가 Supabase + 정적 HTML/GitHub Pages로 완전히 갈아탐. 그 문서의 6절(기술스택)/9절(앱스크립트 주의사항)/미결사항은 전부 무시. **2~3절(설계 철학)은 여전히 유효함.**

## 버그 히스토리 (재발 방지용 — 같은 실수 다시 하지 말 것)
- **2026-08-11**: `guard()` 중첩 충돌 → 저장은 됐는데 화면 안 바뀜 → 유저가 같은 내용 두 번 기록해서 중복 데이터 생김. 과거 데이터에 이 시기 중복이 남아있을 수 있는데 자동으로 못 찾음(유저가 직접 확인해야 함).
- **2026-08-28**: `voucherCache` 레이스 컨디션 → 결제수단 칩이 빈 목록처럼 보임 → 유저가 "머니(쿠팡캐시)가 사라졌다"고 착각하고 "+새 머니"로 중복 등록함. 당시 삭제 기능이 아예 없어서 못 지우고 있었음 — 그날 `deleteVoucher()` 추가로 해결.

## 스키마 변경 이력 (schema.sql 이후, 실제 alter 여부는 유저가 Supabase에서 직접 실행해야 반영됨)
- (2026-08-28 시점까지는 `schema.sql`의 원본 8테이블 구조 그대로. 이후 추가되는 `alter table` 요청은 여기 기록하고, 유저가 실행 완료했는지 체크할 것.)

## 진행 중 / 다음에 할 일
(이 섹션은 `update_log` 할 때마다 최신 상태로 덮어쓸 것 — 과거 이력이 아니라 "지금 어디까지 왔는지" 스냅샷)

**2026-08-28 완료:**
- `editVoucherField`/`editFxField`/`editAnnualField` — `editAssetField`와 같은 필드별 개별 수정 패턴으로 리팩터 완료
- 고정지출: 달러 입력 + 자동 환율 변환(`open.er-api.com`, `localStorage`에 하루 캐싱, 실패 시 최후 fallback 1350원), 토글 켜진 항목 최상단 정렬, 상단 미니 캘린더(`renderFixedCal`), 색 포인트(토글 sea, 합계 sea, USD 태그 sea) 추가. **스키마 변경 필요 — `fixed_cost.currency`, [migration_2026-08-28.sql](migration_2026-08-28.sql) 유저가 Supabase SQL Editor에서 실행해야 작동. 컬럼 없어도 안 터지고 그냥 전부 KRW로 취급됨(안전).**
- 자산-지출 연동(`asset.linked_cash`, `computeDalDalBalance()`) — 매달 1일 그 달 예산(계획 Spending) 기준으로 자동 계산. **스키마 변경 필요 — `asset.linked_cash`, 같은 migration 파일. 컬럼 없으면 기능 자체가 조용히 비활성(안전).**
- 계획 표에 고정지출 활성 항목이 개별로 자동 디폴트 행(`r.auto=true`)으로 뜨게 함 — `loadPlan()`에서 Income 행 바로 뒤에 splice. **주의: 유저 라이브 DB의 `plan` 테이블에 과거 시드 데이터처럼 수동으로 넣은 "고정지출" 집계 행이 남아있으면 이중 계산됨 — 유저가 직접 지워야 함, 내가 안 지움.**
- 계획 탭 이름 "계획" → "관리"로 변경(내부 id/`data-v`는 `plan` 그대로, 표시 텍스트만)
- 자산구성 도넛 — hover `<title>` 툴팁은 남겨뒀지만 주력은 `assetCat` 리스트에 색상 스와치(`DONUT_COLORS`, 카테고리 이름 기준 매핑이라 순서 안 꼬임) + 퍼센티지 추가로 대체
- `openScreen()`에서 서브 화면 열 때마다 `.scroll` 스크롤을 맨 위로 리셋 — 이전 화면 스크롤 위치가 남아서 "로딩 안 된 줄" 착각하던 문제

**남은 것:**
- [ ] "추후 구현" 플레이스홀더(ETF모으기/계좌별배분/아낀비용) — 유저가 불필요하다고 판단, 다음 세션에서 대체 기능 확정되면 교체
- [ ] `migration_2026-08-28.sql` 유저가 Supabase에서 실행했는지 확인 필요 — 안 하면 위 두 기능(달러 환산/달달 연동) 계속 비활성 상태
- [ ] 유저 라이브 `plan` 테이블에 수동 "고정지출" 집계 행 남아있는지 확인 필요(있으면 삭제 요청) — 안 그러면 관리 탭 Expenses 합계가 이중 계산됨
